{
  "name": "pulse-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm -r --parallel dev",
    "start": "open docs/index.html && cd examples/playground && npx vite --port 3200 --open"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
